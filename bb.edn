{:paths ["src" "test"]
 :tasks
 {;; bin/tamaki owns classpath resolution from deps.edn so bb and the JVM
  ;; share one pin set (hil, kotoba-core-contracts, cheshire).
  tamaki {:task (apply shell "bin/tamaki" *command-line-args*)}
  ;; Documented deterministic suite; keeps pins only in deps.edn.
  test {:task (shell "clojure" "-M:test")}}}
